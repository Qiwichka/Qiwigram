/*
 * config.js — подключение к Supabase.
 *
 * Обе строки ниже ПУБЛИЧНЫЕ по устройству Supabase: они лежат в открытом коде
 * любого сайта на этом движке и попадают в браузер каждому посетителю.
 * Прятать их бессмысленно и не нужно — доступ к данным закрывают правила RLS
 * на стороне базы, а не секретность ключа.
 *
 * ЧЕГО ЗДЕСЬ БЫТЬ НЕ ДОЛЖНО НИКОГДА: ключ service_role (он же Secret key).
 * Он обходит все правила разом, и в браузере его быть не может.
 */

window.QIWI = {
    SUPABASE_URL: "https://awedttiuhipaxxrcmpbl.supabase.co",
    SUPABASE_KEY: "sb_publishable_yMMIqlrkm2khvLaJEPy-Bg_caj9E1At",

    /*
     * Вход в мессенджер идёт по нику, а Supabase Auth умеет только по почте.
     * Поэтому в саму авторизацию уходит СИНТЕТИЧЕСКИЙ адрес, собранный из
     * ника: ник «kiwi» -> «kiwi@users.qiwigram.app».
     *
     * Так сделано ради анонимности. Очевидная альтернатива — записывать в
     * авторизацию настоящую почту и перед входом искать её по нику — требует
     * запроса «дай почту вот этого ника», доступного кому угодно без входа.
     * То есть любой желающий смог бы вытащить почту любого пользователя,
     * зная только его ник. Для мессенджера, который обещает анонимность, это
     * дыра, поэтому настоящая почта живёт отдельно, в закрытой таблице, и
     * наружу не отдаётся вообще никогда.
     */
    AUTH_DOMAIN: "users.qiwigram.app",

    /*
     * Адрес сайта, каким его видят посторонние.
     *
     * Нужен ссылкам, которые уходят наружу — в первую очередь приглашениям
     * в чат. Внутри APK `location.origin` равен `https://localhost`: это
     * внутренний адрес самого приложения, годный только ему. Собранное из
     * него приглашение не откроется больше ни у кого, включая того, кому
     * его отправили.
     */
    SITE_URL: "https://qiwigram.vercel.app",

    /** Ограничения на ник — те же, что проверяет база. */
    USERNAME_RE: /^[a-zA-Z0-9_]{3,32}$/,

    /** Сколько сообщений тянем за раз при прокрутке вверх. */
    PAGE_SIZE: 40,

    /** Потолок веса файла до сжатия, байт. Фото жмём, видео отсекаем. */
    MAX_IMAGE_BYTES: 12 * 1024 * 1024,
    MAX_VIDEO_BYTES: 50 * 1024 * 1024,

    /* Всё остальное — документы, архивы, музыка. Не жмётся и не
       разглядывается: что внутри, знает только отправитель. */
    MAX_FILE_BYTES: 50 * 1024 * 1024
}
